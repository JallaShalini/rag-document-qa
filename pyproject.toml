[tool.poetry]
name = "rag-document-qa"
version = "0.1.0"
description = "Retrieval-Augmented Generation document QA system"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.100.0"
uvicorn = "^0.23.0"
python-dotenv = "^1.0.0"
sentence-transformers = "^2.2.2"
chromadb = "^0.4.0"
PyPDF2 = "^3.0.0"
requests = "^2.31.0"

[tool.poetry.dev-dependencies]
pytest = "^8.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
